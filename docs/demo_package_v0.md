# ScrollGuard Demo Package v0

The v0 demo package is a shareable local artifact for review.

## Build commands

- python scripts/build_demo_package.py
- python scripts/build_demo_zip.py

## Main output

outputs/demo_package/scrollguard_v0_demo.zip

## Purpose

The demo package shows the ScrollGuard Year-1 target:

- consume local CT-derived data
- compute features
- score quality and risk
- rank review priority
- preserve evidence
- support human verification
- avoid hallucinated letters or unsupported claims

## Rule

The ZIP is generated output.

Do not commit generated ZIP files unless explicitly requested.
